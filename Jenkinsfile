pipeline {
    agent any
    stages { 
        stage('PWD') {
            steps {
                sh 'pwd'
            }
        }
        stage('git status') {
            steps {
                sh 'git status'
            }
        }
    }
}
